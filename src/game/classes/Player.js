class Player {
  constructor({ id, name, pattern, tilesCount, allyId }) {
    this.id = id
    this.name = name
    this.pattern = pattern
    this.tilesCount = tilesCount
    this.allyId = allyId
  }
}

export default Player
